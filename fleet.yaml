---
name: grafana
defaultNamespace: default
downstreamResources:
  - kind: Secret
    name: test-secret-fleet-ns
helm:
  chart: grafana
  releaseName: grafana
  repo: https://grafana-community.github.io/helm-charts
  values:
    env:
      ENVIRONMENT: lab
  valuesFrom:
    - secretKeyRef:
        name: test-secret-fleet-ns
        key: values.yaml
        namespace: fleet-default
